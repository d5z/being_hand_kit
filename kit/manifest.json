{
  "name": "hand",
  "version": "0.9.6",
  "description": "Hand is a being's hand on the screen. See what's there, click what you want, type what you mean, plan what you need. Works on browsers (Chrome CDP, cross-platform) and macOS native apps. One being, one hand — built by Alice for every being who needs to reach into a GUI. Since 0.7.0 cdp_see returns the accessibility tree by default (role/name/state + stable [idx] handles that cdp_click/cdp_type accept); kind=dom and kind=interactive remain as escape hatches. Receipts distinguish verified vs claimed; tools declare idempotency.",
  "platforms": {
    "supported": [
      "darwin",
      "linux",
      "windows"
    ],
    "backend_matrix": {
      "cdp": [
        "darwin",
        "linux",
        "windows"
      ],
      "ax": [
        "darwin"
      ],
      "vision": [
        "darwin"
      ]
    }
  },
  "command": [
    "bash",
    "start.sh"
  ],
  "tools": [
    {
      "name": "cdp_open",
      "description": "Open Chrome browser (headless). Call first before any browser action.",
      "params": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string"
          }
        }
      },
      "idempotency": "idempotent",
      "evidence": "verified"
    },
    {
      "name": "cdp_close",
      "description": "Close Chrome browser. Clean up when done. The receipt is verified only when the CDP endpoint stops answering after the close.",
      "params": {
        "type": "object",
        "properties": {}
      },
      "idempotency": "idempotent",
      "evidence": "verified"
    },
    {
      "name": "cdp_nav",
      "description": "Navigate to a URL. Returns page title and visible text.",
      "params": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string"
          }
        },
        "required": [
          "url"
        ]
      },
      "idempotency": "idempotent",
      "evidence": "verified"
    },
    {
      "name": "cdp_see",
      "description": "See what's on the current page. Default (kind=a11y) returns the accessibility tree: an indented `role \"name\" (state) [idx]` tree where [idx] is a stable handle for cdp_click/cdp_type. kind=dom returns the visible-text snapshot, kind=interactive the legacy element map (role/text/selector/coordinates), kind=network the request/response flow, kind=screenshot the image path. The receipt carries verified/evidence (node count, serialized bytes, truncation marker, AX source version) and a `hint` when a collapsed menu hides part of the tree.",
      "params": {
        "type": "object",
        "properties": {
          "kind": {
            "type": "string",
            "enum": [
              "a11y",
              "dom",
              "interactive",
              "network",
              "screenshot"
            ]
          }
        }
      },
      "idempotency": "idempotent",
      "evidence": "verified"
    },
    {
      "name": "cdp_click",
      "description": "Click an element on the page. Accepts a CSS selector (e.g. 'a.login') or an [idx] handle from cdp_see(kind=a11y) (e.g. '[93]'), which is resolved through the last snapshot's handle map and verified by re-reading the element box at click time. Also accepts 'text=...' and 'xy:X,Y' (physical pixels).",
      "params": {
        "type": "object",
        "properties": {
          "selector": {
            "type": "string"
          }
        },
        "required": [
          "selector"
        ]
      },
      "idempotency": "side_effect",
      "evidence": "verified"
    },
    {
      "name": "cdp_type",
      "description": "Type text into the currently focused element. Click a field first — including by [idx] handle from cdp_see(kind=a11y) (cdp_click '[93]'), then call this tool. To type into a handle without a separate click, pass '[93]|text' to cdp_click instead — the do router parses the pipe form and focuses the handle's element first; this tool types its argument literally and does not parse the pipe form.",
      "params": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          }
        },
        "required": [
          "text"
        ]
      },
      "idempotency": "append",
      "evidence": "verified",
      "idempotency_note": "append: this tool inserts text at the focus (Input.insertText), so a retry appends twice. The internal fast path cdp_type(selector, text, fast=True) replaces the whole value (el.value = text) and is therefore idempotent; it is not exposed as an MCP tool. Path-split labelling per F15."
    },
    {
      "name": "cdp_shot",
      "description": "Take a screenshot of the current page. Returns the image directly to the model (MCP ImageContent) when the substrate has vision; also includes base64 PNG data.",
      "params": {
        "type": "object",
        "properties": {}
      },
      "idempotency": "idempotent",
      "evidence": "verified"
    },
    {
      "name": "cdp_scroll",
      "description": "Scroll the page. direction: down/up/top/bottom. amount is optional pixels for down/up (default: one screen height).",
      "params": {
        "type": "object",
        "properties": {
          "direction": {
            "type": "string"
          },
          "amount": {
            "type": "integer"
          }
        },
        "required": [
          "direction"
        ]
      },
      "idempotency": "idempotent",
      "evidence": "verified"
    },
    {
      "name": "hand_plan",
      "description": "Tell Hand what you want to accomplish in natural language, and it will plan (and optionally execute) the steps. Like having a pair of hands that knows what to do next.",
      "params": {
        "type": "object",
        "properties": {
          "goal": {
            "type": "string"
          },
          "execute": {
            "type": "boolean"
          }
        },
        "required": [
          "goal"
        ]
      },
      "idempotency": "side_effect",
      "evidence": "claimed"
    },
    {
      "name": "hand_see_vlm",
      "description": "Use a vision language model (VLM) to describe the current screen. Takes a screenshot and sends it to the configured vision model (OpenRouter qwen3-vl by default). Set prompt to ask a specific question about the screen.",
      "params": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string"
          },
          "image_b64": {
            "type": "string"
          }
        }
      },
      "idempotency": "idempotent",
      "evidence": "claimed"
    },
    {
      "name": "hand_see_ocr",
      "description": "Read exact on-screen text via Apple Vision OCR (zh-Hans/zh-Hant/en/ja). Complementary to hand_see_vlm (VLM describes what-is-this) — this reads what-does-it-say. Pass query to locate a specific string: returns matching lines with pixel coordinates (x/y/w/h/center_x/center_y) for subsequent clicking.",
      "params": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string"
          }
        }
      },
      "idempotency": "idempotent",
      "evidence": "claimed"
    },
    {
      "name": "health",
      "description": "Check if Hand is alive and well — uptime, usage count, Chrome status.",
      "params": {
        "type": "object",
        "properties": {}
      },
      "idempotency": "idempotent",
      "evidence": "claimed"
    }
  ]
}